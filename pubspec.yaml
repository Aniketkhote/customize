name: customize
description: Customize Flutter is awesome package to create beautiful UI.
version: 0.0.4
homepage: "https://github.com/Aniketkhote/customize"

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^0.19.3
  get: ^3.26.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.5.3

flutter: null
